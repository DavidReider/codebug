function createCardStates(opts, base, dark, name, solid, muted) {
  return {
    enabled: opts.card({
      base,
      dark,
      name,
      state: "enabled",
      solid: solid.default,
      muted: muted.default
    }),
    disabled: opts.card({
      base,
      dark,
      name,
      state: "disabled",
      solid: solid.default,
      muted: muted.default
    }),
    hovered: opts.card({
      base,
      dark,
      name,
      state: "hovered",
      solid: solid.default,
      muted: muted.default
    }),
    pressed: opts.card({
      base,
      dark,
      name,
      state: "pressed",
      solid: solid.default,
      muted: muted.default
    }),
    selected: opts.card({
      base,
      dark,
      name,
      state: "selected",
      solid: solid.default,
      muted: muted.default
    })
  };
}
export {
  createCardStates
};
